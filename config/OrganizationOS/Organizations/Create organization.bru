meta {
  name: Create organization
  type: http
  seq: 2
}

post {
  url: http://{{URL}}/organizations?name=Dunder1
  body: none
  auth: bearer
}

query {
  name: Dunder1
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
