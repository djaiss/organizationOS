### Permissions

OrgOS lets you manage permissions for your organization at a very granular level.

A Permission (`administrator` for instance) is associated with actions (called OrganizationAction, like `manage_users`).

Then, a user is assigned to a permission.
